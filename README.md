neoclock
========

A ws2812 clock

## Installation

1. Run the following (adjust for your system):

  ```bash
  $ brew install arduino-cli
  ```

2. Edit `vars.mak` and update any values to match your system.

2. Plug in your board and run `make`.
